# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Committed on purpose. See docker/postgres/init/setup-test-role.sql).
# The database named here is `spacecraft`, not `spacecraft_test`: doctrine.yaml
# appends the `_test` suffix under when@test (and a ParaTest token when set).
DATABASE_URL="postgresql://spacecraft_test:iamnotasecret@database:5432/spacecraft?serverVersion=18&charset=utf8"
